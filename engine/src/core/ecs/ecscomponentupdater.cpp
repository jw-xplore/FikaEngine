#include "ecscomponentupdater.h"
#include "core/ecs/ecsmanager.h"
#include "core/ecs/ecscomponent.h"
#include "core/systemsHolder.h"
#include <iostream>
#include <cassert>

namespace FikaECS
{
	/*
	template <typename T>
	ComponentUpdater<T>::ComponentUpdater(unsigned int componentId)
	{
		/*
		if (componentId == 0)
			std::cout << "Component id 0 is invalid. Use component id to create matching system. \n";

		assert(componentId != 0);

		// Setup
		targetComponentId = componentId;

		components("ECS TestCmp", 256);
		*/
	//}

	/*
	ComponentUpdater::ComponentUpdater(unsigned int componentId)
	{
		if (componentId == 0)
			std::cout << "Component id 0 is invalid. Use component id to create matching system. \n";

		assert(componentId != 0);

		// Setup
		targetComponentId = componentId;

		components = std::make_unique<PoolAllocator<ECSComponent>>("ECS TestCmp", 256);
	}
	*/
}